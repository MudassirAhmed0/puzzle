{
  "scripts": {
    "start": "npx tailwindcss -i ./assets/styles/input.css -o ./assets/styles/output.css --watch",
    "server": "json-server --watch ./server/products.json -p 4000 && json-server --watch ./server/categories.json -p 3000"
  },
  "devDependencies": {
    "postcss": "^8.4.23",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0",
    "tailwindcss": "^3.3.1"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
